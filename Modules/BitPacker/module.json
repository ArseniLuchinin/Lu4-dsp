{
  "modules": [
    {
      "name": "BitPacker",
      "description": "Упаковывает поток битов в байты. Входные данные — байты, где каждый байт хранит один бит. Выход — компактные байты (8 бит на байт). Поддерживает отбрасывание начальных битов и упаковку хвоста.",
      "fields": [
        {
          "name": "bit order",
          "type": "enum",
          "label": "Порядок бит: msb-first"
        },
        {
          "name": "flush tail",
          "type": "bool",
          "label": "Упаковывать остаток в дополнительный байт"
        },
        {
          "name": "discard leading bits",
          "type": "int",
          "label": "Сколько начальных битов отбросить"
        }
      ]
    }
  ]
}
