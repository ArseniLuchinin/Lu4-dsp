# PhaseRotator

Фазовое вращение комплексного сигнала.

Получает фазу от CarrierPhaseEstimator по тегу и вращает все семплы на `exp(j * phase)`.

## Параметры

| Параметр | Тип | Описание |
|---|---|---|
| `phase tag` | string | Тег для получения фазы (по умолчанию `qpsk_phase`) |
